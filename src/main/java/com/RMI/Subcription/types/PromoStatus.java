package com.RMI.Subcription.types;
public enum PromoStatus {
    ACTIVE,       // Le code promo est actif
    EXPIRED,      // Le code promo est expiré
    USED,         // Le code promo a été utilisé
    DISABLED      // Le code promo est désactivé
}
